<Page
    x:Class="LykkeWalletApp.Views.Kyc.RestrictedAreaView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LykkeWalletApp.Views.Kyc"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:LykkeWalletApp.Controls"
   d:DataContext="{d:DesignInstance local:RestrictedAreaView}"
    mc:Ignorable="d">

    <StackPanel Background="{StaticResource AppBackground}" Padding="10">
        <Grid Height="200">
            <controls:Icon1View HorizontalAlignment="Center" VerticalAlignment="Center" Width="150" Height="120"/>
        </Grid>
        <TextBlock Style="{StaticResource InfoHeader}" Text="Oops" />

        <TextBlock HorizontalAlignment="Center" Margin="5" TextWrapping="WrapWholeWords" TextAlignment="Center" Text="{Binding Model.FullName}"/>
    </StackPanel>
</Page>
